import {AlertTriangle,CalendarDays,CircleDollarSign,Globe2,Users,Wrench} from 'lucide-react';
import {attentionItems,costMix,siteCosts} from '../lib/demo-data';
const money=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0});
function Stat({label,value,detail,icon:Icon}:{label:string;value:string;detail:string;icon:React.ComponentType<{size?:number}>}){return <article className="stat-card"><div className="stat-icon"><Icon size={18}/></div><div><div className="eyebrow">{label}</div><div className="stat-value">{value}</div><div className="muted">{detail}</div></div></article>}
export default function Home(){const actual=siteCosts.reduce((s,x)=>s+x.actual,0);const committed=siteCosts.reduce((s,x)=>s+x.committed,0);const forecast=siteCosts.reduce((s,x)=>s+x.forecast,0);return <main className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">M</span><div><strong>Service Ops</strong><span>Global Command</span></div></div><nav><a className="active"><Globe2 size={17}/>Command Center</a><a><CalendarDays size={17}/>Schedule</a><a><Wrench size={17}/>Work Orders</a><a><Users size={17}/>Technicians</a><a><CircleDollarSign size={17}/>Financials</a></nav><div className="sidebar-note">Global service operations, workforce capacity, dispatch, and site-level cost visibility.</div></aside><section className="content"><header className="page-head"><div><div className="eyebrow brand-text">GLOBAL SERVICE OPERATIONS</div><h1>Command Center</h1><p>What needs attention across the service organization right now.</p></div><button>New work order</button></header><div className="stats-grid"><Stat label="Technicians" value="90" detail="74 available today" icon={Users}/><Stat label="Active visits" value="18" detail="7 starting this week" icon={Wrench}/><Stat label="At risk" value="3" detail="2 currently unstaffed" icon={AlertTriangle}/><Stat label="Forecast spend" value={money.format(forecast)} detail={`${money.format(actual)} actual`} icon={CircleDollarSign}/></div><div className="two-col"><section className="panel"><div className="panel-head"><div><div className="eyebrow">FINANCIAL OPERATIONS</div><h2>Site spend</h2></div><span>{money.format(committed)} committed</span></div><div className="table-wrap"><table><thead><tr><th>Site</th><th>Region</th><th>Actual</th><th>Committed</th><th>Forecast</th><th>Budget</th></tr></thead><tbody>{siteCosts.map(r=><tr key={r.site}><td><strong>{r.site}</strong></td><td>{r.region}</td><td>{money.format(r.actual)}</td><td>{money.format(r.committed)}</td><td>{money.format(r.forecast)}</td><td>{money.format(r.budget)}</td></tr>)}</tbody></table></div></section><section className="panel"><div className="panel-head"><div><div className="eyebrow">COST MIX</div><h2>What we are spending on</h2></div></div><div className="cost-list">{costMix.map(i=><div key={i.label} className="cost-row"><div><span>{i.label}</span><strong>{i.value}%</strong></div><div className="bar"><span style={{width:`${i.value}%`}}/></div></div>)}</div></section></div><div className="two-col lower"><section className="panel"><div className="panel-head"><div><div className="eyebrow">REGIONAL CAPACITY</div><h2>Technician utilization</h2></div></div><div className="region-grid"><div><span>Americas</span><strong>82%</strong></div><div className="warn"><span>EMEA</span><strong>91%</strong></div><div><span>APAC</span><strong>67%</strong></div></div></section><section className="panel attention"><div className="panel-head"><div><div className="eyebrow">NEEDS ATTENTION</div><h2>Operational risks</h2></div></div>{attentionItems.map(i=><div className={`alert ${i.level}`} key={i.title}><AlertTriangle size={17}/><div><strong>{i.title}</strong><span>{i.detail}</span></div></div>)}</section></div></section></main>}
